services:
  app:
    build:
      context: .
      target: development
    environment:
      NODE_ENV: development
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run start:dev
    restart: 'no'
